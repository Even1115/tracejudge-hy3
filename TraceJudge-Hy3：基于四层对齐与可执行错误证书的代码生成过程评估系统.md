# TraceJudge-Hy3：基于四层对齐与可执行错误证书的代码生成过程评估系统

**参赛课题：** 课题 2——可验证场景：过程评估与错误定位  
**应用方向：** 代码任务（Code Task）  
**项目定位：** 面向 AI 生成代码的细粒度过程评估与首错定位系统  

---

## 一、项目摘要

当前代码生成模型的主流评估方式通常以“代码是否通过测试”为主要标准。这种评估方式能够判断程序在既有测试用例上的行为，却无法可靠回答以下问题：

1. 模型是否正确理解了需求；
2. 模型给出的解题思路是否成立；
3. 代码是否真正实现了其声称的方案；
4. 错误最早从哪个推理步骤或代码位置开始出现；
5. 代码是否只是碰巧通过有限测试，实际逻辑仍然存在缺陷；
6. 最终结果正确时，推理过程是否能够合理支撑该结果。

本项目拟基于 Hy3 构建一个面向 Python 函数级代码任务的过程评估应用，提出一种“**需求—推理—代码—执行证据**”四层对齐方法，对 AI 生成代码进行多层一致性分析。

系统不仅判断代码是否通过测试，还将：

- 将自然语言需求拆解为可验证的需求条款；
- 将需求条款与模型推理步骤建立对应关系；
- 将推理步骤与实际代码区域建立映射；
- 结合静态分析、沙盒执行、隐藏测试和自动生成反例验证错误；
- 定位首个具有因果意义的错误步骤；
- 输出错误类型、相关代码位置和可复现反例；
- 识别“代码结果正确但推理过程错误”“思路正确但实现偏离”“可见测试通过但逻辑投机”等特殊样本。

最终系统将为确认的错误生成一份“**可执行错误证书**”，使过程评估结果不仅是大模型的主观判断，而是能够通过输入、期望输出、实际输出和执行轨迹进行复现。

官方课题要求应用能够输出完整解答过程，并实现过程正确性判定、错误步骤定位、错误类型归类、结果正确但过程不成立的样本识别，同时通过定位准确率和误报率验证过程评估器的可靠性。本项目在满足这些要求的基础上，进一步强化评估结果的可验证性和因果可追踪性。

---

## 二、项目背景与问题定义

### 2.1 背景

大语言模型已经能够根据自然语言需求生成完整代码，但在实际应用中，“代码能否运行”与“代码是否真正正确”并不等价。

传统代码生成评测一般采用如下流程：

```text
输入需求
   ↓
模型生成代码
   ↓
运行测试用例
   ↓
全部通过则判定正确
```

这种方式主要验证有限输入上的输出行为，难以判断模型是否“对因而对”。

例如，一段代码可能出现以下情况：

- 只针对可见测试用例进行硬编码；
- 遗漏未被测试覆盖的边界条件；
- 推理过程错误，但最终代码碰巧正确；
- 推理过程正确，但实际代码没有实现对应逻辑；
- 两个计算或逻辑错误相互抵消，得到正确输出；
- 使用错误算法，但在小规模测试上仍然通过；
- 声称处理了某个条件，但代码中不存在相应分支；
- 功能输出正确，但不满足复杂度或资源约束。

因此，仅依赖 pass/fail 无法完整评估 AI 生成代码的质量。

### 2.2 待解决的核心问题

本项目重点解决以下四类问题。

#### 问题一：如何判断生成过程是否成立

模型输出通常包含需求理解、算法设计、边界条件分析、实现步骤和最终代码。系统需要判断这些内容是否：

- 正确理解题目目标；
- 覆盖关键需求条件；
- 使用适用的算法或数据结构；
- 前后逻辑一致；
- 不存在无法支撑的跳步；
- 不包含虚构条件或代码中未实现的逻辑。

#### 问题二：如何定位首个错误，而不是只定位最终症状

代码运行失败的位置不一定是错误真正产生的位置。

例如：

```text
Step 2：错误理解重复元素的处理规则
Step 3：基于错误理解设计状态转移
Step 4：按照错误方案编写代码
第 18 行：测试结果错误
```

第 18 行只是错误的表现位置，真正的根因发生在 Step 2。

因此，本项目需要区分：

- **根因步骤**：第一次引入错误假设或错误结论的位置；
- **症状位置**：错误最终在代码或执行结果中表现的位置；
- **受影响步骤**：由前序错误导致的后续错误。

#### 问题三：如何识别“结果正确但过程错误”

最终代码通过测试，并不意味着过程正确。

典型情况包括：

- reasoning 中使用了错误算法，但代码由其他方式生成且结果正确；
- reasoning 声称使用动态规划，代码实际采用暴力枚举；
- 代码通过有限测试，但存在硬编码或针对样例特判；
- 推理步骤不成立，但错误恰好没有影响当前输入；
- 代码只适用于当前数据范围，不适用于题目声明的一般情况。

#### 问题四：如何降低 LLM-as-judge 的误报

直接让大模型阅读需求和代码后判断错误，可能产生以下问题：

- 把与参考答案不同的合法实现误判为错误；
- 提出无法复现的边界缺陷；
- 根据失败结果事后编造原因；
- 忽略代码中的等价写法；
- 同一份答案多次评估结果不稳定。

因此，过程评估器不应只输出自然语言判断，还需要尽可能提供静态或动态证据。

---

## 三、目标用户与应用场景

### 3.1 目标用户

本项目主要面向以下用户：

1. 使用大模型辅助编程、需要审查 AI 生成代码可靠性的开发者；
2. 需要评估学生解题思路和代码实现过程的编程教学场景；
3. 需要分析代码生成模型失败模式和能力边界的模型评测团队；
4. 需要建立 AI Coding 质量保障流程的研发团队。

### 3.2 典型应用场景

#### 场景一：AI 编程助手结果复核

开发者输入需求后，AI 输出解题思路和代码。TraceJudge-Hy3 自动检查：

- 是否遗漏需求；
- reasoning 与代码是否一致；
- 是否存在未覆盖的边界条件；
- 是否能够生成触发缺陷的反例。

#### 场景二：代码教学与作业批改

系统不仅判断学生代码是否通过测试，还可以指出：

- 是题意理解错误；
- 是算法设计错误；
- 是边界条件遗漏；
- 还是代码实现与正确思路不一致。

#### 场景三：模型能力评测

对 Hy3 在不同难度、不同错误类型和不同任务结构下的表现进行分层分析，识别模型性能开始明显下降的临界区间。

### 3.3 引入 Hy3 的必要性

传统静态分析工具擅长发现语法错误、类型问题、未使用变量和部分控制流缺陷，但难以判断：

- 自然语言需求是否被正确理解；
- reasoning 是否能够支撑当前实现；
- 代码是否完成了 reasoning 中声称的设计；
- 多种合法算法是否都符合题目要求；
- 错误属于需求、设计、实现还是执行层。

Hy3 具备自然语言理解和代码理解能力，适合承担：

- 需求条款提取；
- 解题过程生成；
- 推理步骤审查；
- 需求—推理—代码映射；
- 疑似缺陷和反例候选生成；
- 错误类型归纳与解释。

但 Hy3 的判断仍需由规则、静态分析和执行结果进行交叉验证，以降低单纯依赖 LLM-as-judge 带来的主观性。

---

## 四、项目目标与范围

### 4.1 总体目标

构建一个端到端、可运行、可复现的代码生成过程评估系统，实现：

```text
题目输入
  ↓
Hy3 生成结构化解答过程与代码
  ↓
需求、推理、代码结构化解析
  ↓
沙盒执行与测试验证
  ↓
四层一致性分析
  ↓
疑似错误定位
  ↓
反例生成与执行验证
  ↓
首错步骤、错误类型和错误证书输出
```

### 4.2 项目范围

为确保在活动周期内完成可靠实验，本项目限定为：

| 项目维度 | 本阶段范围 |
|---|---|
| 编程语言 | Python |
| 任务粒度 | 函数级代码生成任务 |
| 输入形式 | 自然语言需求、函数签名、可见示例 |
| 输出形式 | 结构化 reasoning、Python 代码、过程评估报告 |
| 执行环境 | Docker 或受限 Python 子进程 |
| 基础题目来源 | HumanEval、MBPP 等公开代码任务 |
| 主要评估目标 | 功能正确性、过程正确性、计划—代码一致性、错误定位 |
| 暂不覆盖 | 仓库级代码修改、多文件项目、完整自动修复、大规模多语言评测 |

### 4.3 项目不以复杂多 Agent 为目标

本项目整体采用可复现的评测 Workflow，而不是为了形式上体现 Agent 而堆叠多个角色。

Hy3 可以在部分环节自主调用测试、静态分析和反例验证工具，但系统主流程保持固定：

```text
生成 → 解析 → 执行 → 评估 → 验证 → 汇总
```

这样有利于：

- 控制变量；
- 复现实验；
- 定位模块误差；
- 完成消融实验；
- 准确计算定位率和误报率。

---

## 五、核心创新与项目贡献

### 5.1 创新点一：需求—推理—代码—执行证据四层对齐

本项目将代码生成过程划分为四个层次。

```text
第一层：需求条款 Requirement
第二层：推理步骤 Reasoning
第三层：代码区域 Code
第四层：执行证据 Execution Evidence
```

系统建立如下映射：

```text
需求 R3：空列表输入时返回 0
       ↓
推理 S4：单独处理空输入
       ↓
代码 L18-L21：空输入判断分支
       ↓
执行 E2：输入 []，期望 0，实际抛出 IndexError
```

与直接让 LLM 对整段回答给出“正确或错误”相比，四层对齐能够明确回答：

- 哪一条需求没有被满足；
- 哪个推理步骤声称处理了该需求；
- 对应代码是否真正实现；
- 执行结果是否支持这一判断。

### 5.2 创新点二：面向错误结论的可执行错误证书

过程评估器发现疑似错误后，不直接将其判定为确认错误，而是尝试生成能够触发该问题的反例。

验证流程如下：

```text
Hy3 提出疑似缺陷
       ↓
生成候选反例
       ↓
参考实现与候选实现差分执行
       ↓
沙盒复现差异
       ↓
缩减反例
       ↓
输出确认错误及证据
```

确认后的错误报告包含：

```json
{
  "verdict": "confirmed_bug",
  "violated_requirement": "R3",
  "first_faulty_step": "S4",
  "faulty_layer": "implementation",
  "code_span": "L18-L21",
  "error_type": "boundary_condition_omission",
  "counterexample": {
    "input": [],
    "expected": 0,
    "actual": "IndexError"
  },
  "evidence_status": "executed"
}
```

若系统无法通过执行复现，只能将其标记为：

```text
unverified_suspicion
```

而不是直接作为确认错误统计，从而降低误报。

### 5.3 创新点三：首个因果错误定位

本项目不只定位失败测试对应的代码行，而是定位“最早引入错误的因果步骤”。

定义如下：

> 首个因果错误是解答过程中最早出现的、无法由题目条件或前序正确步骤支持，并且对后续错误结果产生因果影响的步骤。

评估结果同时记录：

```json
{
  "root_cause_step": "S2",
  "symptom_code_span": "L18-L21",
  "affected_steps": ["S3", "S4", "S5"]
}
```

这可以区分：

- 需求理解错误；
- 算法方案错误；
- 正确方案下的实现偏差；
- 正确实现下的输出格式问题。

### 5.4 创新点四：反事实配对过程评测集

本项目将在已有代码 benchmark 基础上构建反事实配对样本。

每一组样本尽量只改变一个变量，以验证评估器是否真正理解过程，而不是只根据测试结果猜测。

| 配对类型 | 保持不变 | 修改内容 | 验证目标 |
|---|---|---|---|
| reasoning 反事实 | 代码与执行结果 | 将正确 reasoning 改为错误 reasoning | 识别代码正确但过程错误 |
| code 反事实 | 正确 reasoning | 在代码中注入实现缺陷 | 识别思路正确但实现偏离 |
| shortcut 反事实 | 可见测试结果 | 将通用实现改为硬编码或特判 | 识别投机实现 |
| equivalent 反事实 | 功能和需求 | 替换为另一种合法算法 | 检验是否误报非参考路径 |
| boundary 反事实 | 主体逻辑 | 删除一个边界条件分支 | 检验首错定位能力 |

### 5.5 创新点五：模块级消融验证

本项目不会只展示完整系统的单一准确率，而是比较不同评估方法：

1. 仅测试执行；
2. 直接 LLM-as-judge；
3. LLM + 四层对齐；
4. LLM + 四层对齐 + 静态证据；
5. 完整方法：静态证据 + 动态执行 + 可执行反例。

通过消融实验分析：

- 哪个模块提升了错误检测能力；
- 哪个模块提高了定位准确率；
- 哪个模块降低了误报率；
- 可执行证据是否能过滤 LLM 的虚假缺陷判断。

---

## 六、评估对象的三维定义

原有“答案正确/错误 × 过程正确/错误”的二维划分，在代码任务中仍然较粗。

本项目将评估拆分为三个维度。

### 6.1 推理正确性

判断模型是否正确完成：

```text
需求理解 → 算法设计 → 边界分析 → 实现计划
```

主要关注：

- 需求是否被正确拆解；
- 算法是否满足问题约束；
- 步骤是否逻辑成立；
- 是否遗漏必要条件；
- 是否虚构不存在的条件；
- 复杂度分析是否合理。

### 6.2 计划—代码一致性

判断代码是否真正实现 reasoning 声称的方案。

例如：

```text
Reasoning：使用集合记录已经访问的元素
Code：实际使用列表并线性查找
```

这不一定导致功能错误，但说明模型的过程记录无法真实解释代码。

### 6.3 功能正确性

通过沙盒执行判断候选代码是否满足题目要求，包括：

- 可见测试；
- 隐藏测试；
- 边界测试；
- 自动生成反例；
- 超时与资源限制；
- 输出格式与函数接口。

### 6.4 三维组合结果

| 推理正确 | 计划—代码一致 | 功能正确 | 判定 |
|---|---|---|---|
| 是 | 是 | 是 | 完全正确 |
| 否 | 否或不确定 | 是 | 结果正确但过程不成立 |
| 是 | 否 | 否 | 思路正确但实现偏离 |
| 否 | 是 | 否 | 按错误思路实现 |
| 表面合理 | 否 | 可见测试通过 | 特判或投机实现 |
| 是 | 是 | 否 | 局部实现、格式或运行时错误 |
| 是 | 是 | 是 | 合法等价实现，不应误报 |

---

## 七、系统总体架构

```text
┌──────────────────────────────────────┐
│ 评测题集                              │
│ 需求 + 函数签名 + 可见测试 + 隐藏测试 │
│ 标准实现 + 难度标签 + 需求条款         │
└──────────────────┬───────────────────┘
                   ↓
┌──────────────────────────────────────┐
│ A. Hy3 Solver                        │
│ 需求理解 / 算法设计 / 边界分析        │
│ 分步实现计划 / 最终代码               │
└──────────────────┬───────────────────┘
                   ↓
┌──────────────────────────────────────┐
│ B. Trace Parser                      │
│ 结构化解析 Requirement / Step / Code │
│ 建立初始步骤—代码映射                 │
└──────────────┬───────────────┬───────┘
               ↓               ↓
┌──────────────────────┐  ┌──────────────────────┐
│ C. Static Analyzer   │  │ D. Sandbox Executor  │
│ AST / 分支 / 循环     │  │ 可见与隐藏测试        │
│ 函数调用 / 复杂度线索 │  │ 异常 / 超时 / 执行轨迹 │
└──────────────┬───────┘  └──────────────┬───────┘
               └──────────────┬───────────┘
                              ↓
┌──────────────────────────────────────┐
│ E. Four-Layer Process Evaluator      │
│ 需求—推理—代码—执行四层一致性评估     │
│ 首个错误步骤与错误类型预测             │
└──────────────────┬───────────────────┘
                   ↓
┌──────────────────────────────────────┐
│ F. Counterexample Generator          │
│ 属性测试 / 差分测试 / 边界输入生成     │
│ 反例执行与最小化                      │
└──────────────────┬───────────────────┘
                   ↓
┌──────────────────────────────────────┐
│ G. Evidence Aggregator               │
│ confirmed / suspected / cleared      │
│ 可执行错误证书                        │
└──────────────────┬───────────────────┘
                   ↓
┌──────────────────────────────────────┐
│ H. Evaluation Report                 │
│ 功能正确率 / 过程正确率 / 定位准确率   │
│ 错误类型 / 难度分层 / 典型案例         │
└──────────────────────────────────────┘
```

---

## 八、核心模块设计

## 8.1 题目适配与需求条款提取模块

每道题首先被转换为统一格式。

```json
{
  "problem_id": "p001",
  "requirement": "实现一个函数，返回数组中第一个缺失的正整数。",
  "function_signature": "def first_missing_positive(nums: list[int]) -> int:",
  "visible_test_cases": [
    {
      "input": [[1, 2, 0]],
      "expected": 3
    }
  ],
  "hidden_test_cases": [],
  "reference_solution": "...",
  "difficulty": "medium",
  "source": "public_benchmark",
  "tags": [
    "array",
    "in-place-hash",
    "boundary"
  ]
}
```

Hy3 或规则模块进一步将自然语言需求拆分为需求条款：

```json
{
  "requirements": [
    {
      "id": "R1",
      "content": "返回最小的缺失正整数"
    },
    {
      "id": "R2",
      "content": "忽略零和负数"
    },
    {
      "id": "R3",
      "content": "正确处理重复元素"
    },
    {
      "id": "R4",
      "content": "满足题目声明的复杂度约束"
    }
  ]
}
```

需求条款是后续过程对齐和错误归因的基本单位。

---

## 8.2 Hy3 解题应用

Hy3 接收：

- 题目需求；
- 函数签名；
- 可见测试；
- 输出格式约束。

系统要求 Hy3 输出可审查的结构化解答，而不是只输出代码。

```json
{
  "problem_id": "p001",
  "requirement_understanding": {
    "goal": "...",
    "constraints": ["..."],
    "input_output_contract": "..."
  },
  "design_choices": {
    "algorithm": "...",
    "data_structures": ["..."],
    "complexity": {
      "time": "O(n)",
      "space": "O(1)"
    }
  },
  "edge_cases_considered": [
    "空数组",
    "全部为负数",
    "存在重复元素"
  ],
  "implementation_steps": [
    {
      "step_id": "S1",
      "content": "过滤与结果无关的数值",
      "related_requirements": ["R2"]
    },
    {
      "step_id": "S2",
      "content": "将有效正整数放到对应下标位置",
      "related_requirements": ["R1", "R3"]
    }
  ],
  "code": "..."
}
```

模型不需要公开不可见的内部思维过程，只需要生成面向用户、可验证的解题说明与实现步骤。

---

## 8.3 静态分析模块

静态分析模块使用 Python AST 和规则提取以下证据：

- 是否存在需求声称的关键分支；
- 是否使用了声明的数据结构；
- 循环结构和嵌套层数；
- 函数调用关系；
- 返回路径；
- 异常处理；
- 硬编码常量或样例特征；
- 输出格式；
- 部分复杂度线索；
- reasoning 中声明的操作是否能在代码中找到对应结构。

静态分析不独立判断代码语义是否正确，而是为四层评估器提供辅助证据。

示例：

```json
{
  "static_evidence": {
    "has_empty_input_branch": false,
    "uses_set": false,
    "nested_loop_depth": 2,
    "hardcoded_literals": [3, 5, 7],
    "all_return_paths": ["L12", "L25"]
  }
}
```

---

## 8.4 沙盒执行模块

生成代码将在隔离环境中运行。

安全与资源限制包括：

- 禁止网络访问；
- 限制 CPU 时间；
- 限制内存；
- 限制进程数；
- 限制文件访问；
- 设置单测试超时；
- 捕获标准输出、标准错误和异常；
- 对死循环或资源异常单独分类。

执行结果结构：

```json
{
  "visible_tests": {
    "passed": 3,
    "total": 3
  },
  "hidden_tests": {
    "passed": 7,
    "total": 10
  },
  "failures": [
    {
      "input": [],
      "expected": 0,
      "actual": "IndexError",
      "exception": "IndexError"
    }
  ],
  "runtime_status": "completed"
}
```

隐藏测试不向 Solver 提供，避免模型直接针对已知用例生成特判。

---

## 8.5 四层过程评估器

过程评估器按照以下顺序进行检查。

### 第一阶段：需求—推理对齐

对每个需求条款判断：

- 是否被 reasoning 覆盖；
- 是否被正确理解；
- 是否存在条件遗漏；
- 是否引入额外假设；
- 推理是否满足适用条件。

### 第二阶段：推理内部一致性

检查：

- 当前步骤能否由前一步推出；
- 是否发生无依据跳步；
- 是否存在前后矛盾；
- 算法选择是否满足约束；
- 边界分析是否完整；
- 复杂度结论是否与方案一致。

### 第三阶段：推理—代码对齐

检查：

- reasoning 中的关键步骤是否在代码中实现；
- 代码是否引入 reasoning 未说明的重要逻辑；
- 算法和数据结构是否一致；
- 边界条件是否存在对应分支；
- 复杂度是否与声明一致；
- 是否存在“口头上处理了，代码中没有处理”的情况。

### 第四阶段：代码—执行对齐

检查：

- 测试结果是否符合需求；
- 失败输入对应哪个需求条款；
- 执行轨迹能否支持当前错误归因；
- 异常是否由前序推理或实现错误引起；
- 可见测试通过时，隐藏测试和反例是否揭示缺陷。

评估器输出：

```json
{
  "final_functional_correct": false,
  "reasoning_correct": true,
  "plan_code_aligned": false,
  "process_correct": false,
  "first_faulty_layer": "implementation",
  "first_faulty_step": "S3",
  "code_span": "L15-L19",
  "violated_requirement": "R3",
  "error_type": "plan_code_mismatch",
  "evidence_status": "candidate"
}
```

---

## 8.6 反例生成与验证模块

对于评估器提出的疑似错误，系统尝试生成验证输入。

反例来源包括：

1. 根据遗漏需求生成边界输入；
2. 根据失败分支生成邻近输入；
3. 基于参考实现与候选实现进行差分测试；
4. 根据输入约束生成随机或属性测试；
5. 对已有失败输入进行最小化；
6. 针对硬编码常量生成相邻值或结构变体。

验证逻辑：

```text
候选输入
   ↓
运行参考实现
   ↓
运行候选实现
   ↓
比较输出、异常与资源行为
   ↓
存在差异 → 确认错误
不存在差异 → 继续搜索或保留为未验证怀疑
```

错误状态分为：

| 状态 | 含义 |
|---|---|
| `confirmed_bug` | 已由可执行反例复现 |
| `strongly_supported` | 有测试或静态证据，但尚无最小反例 |
| `unverified_suspicion` | 仅由 LLM 提出，尚无外部证据 |
| `cleared` | 后续验证表明原判断不成立 |

---

## 九、错误分类体系

错误分类按照错误首次发生的层级进行设计。

| 错误代码 | 错误类型 | 所属层级 | 判定标准 |
|---|---|---|---|
| R01 | 需求目标误读 | 需求 | 对函数目标、输入或输出含义理解错误 |
| R02 | 必要条件遗漏 | 需求 | 未考虑题目明确声明的条件 |
| R03 | 额外假设或幻觉 | 需求 | 引入题目未提供且不成立的假设 |
| P01 | 算法或概念错误 | 推理 | 所选算法无法一般性解决问题 |
| P02 | 无依据跳步 | 推理 | 结论无法由前序条件或步骤支持 |
| P03 | 复杂度判断错误 | 推理 | 声明的复杂度与方案或代码不一致 |
| A01 | 计划—代码不一致 | 对齐 | 代码未实现 reasoning 中声称的关键步骤 |
| A02 | 未解释的关键实现 | 对齐 | 代码引入重要逻辑但 reasoning 未说明 |
| C01 | 边界条件错误 | 代码 | 空输入、极值、重复值等处理错误 |
| C02 | 控制流或逻辑错误 | 代码 | 分支、循环、状态更新等实现错误 |
| C03 | 数据结构使用错误 | 代码 | 数据结构操作与算法意图不符 |
| C04 | 输出或接口不符 | 代码 | 函数签名、返回类型或格式不满足要求 |
| C05 | 硬编码或投机实现 | 代码 | 依赖特定样例或有限输入特征 |
| E01 | 运行时异常 | 执行 | 出现未处理异常 |
| E02 | 超时或资源异常 | 执行 | 算法无法在限制内完成 |
| E03 | 数值或精度错误 | 执行 | 浮点、溢出或精度处理错误 |

每个错误样本只标注一个主要根因类型，同时允许记录多个次级症状。

---

## 十、评测题集设计

### 10.1 数据组成

本项目题集由三部分组成。

#### 第一部分：基础功能题集

从 HumanEval、MBPP 等公开函数级代码生成 benchmark 中筛选题目，统一为项目数据格式。

用途：

- 测试 Hy3 代码生成能力；
- 统计最终功能正确率；
- 分析不同难度下的过程正确率；
- 生成自然模型错误样本。

#### 第二部分：反事实配对挑战集

在基础题目及其正确解答上构造控制变量样本：

- reasoning 错误、代码正确；
- reasoning 正确、代码错误；
- 可见测试通过、隐藏行为错误；
- 硬编码或样例特判；
- 不同但合法的正确算法；
- 合理跳步但过程仍然成立；
- 两个错误抵消后结果正确。

用途：

- 评估“答案正确但过程错误”的识别能力；
- 检验评估器是否过度依赖测试结果；
- 检验对合法等价实现的误报率。

#### 第三部分：人工标注验证集

从自然生成样本和反事实样本中选取代表性轨迹，由人工标注：

```json
{
  "response_id": "r001",
  "functional_correct": true,
  "reasoning_correct": false,
  "plan_code_aligned": false,
  "first_faulty_layer": "reasoning",
  "first_faulty_step": "S2",
  "error_type": "P01",
  "human_rationale": "该算法不适用于存在重复元素的输入",
  "affected_steps": ["S3", "S4"]
}
```

用途：

- 验证错误检测准确率；
- 验证首错定位准确率；
- 计算错误分类一致性；
- 统计误报率。

### 10.2 建议规模

#### 最小可交付版本

| 数据部分 | 计划规模 |
|---|---:|
| 基础题目 | 60 道 |
| 简单题 | 20 道 |
| 中等题 | 20 道 |
| 困难题 | 20 道 |
| 每题生成轨迹 | 至少 1 条 |
| 反事实配对样本 | 30 组左右 |
| 人工标注轨迹 | 40—60 条 |
| 错误类型 | 8—12 类 |

#### 扩展版本

在时间和 API 预算允许的情况下：

- 基础题目扩展至 100 道；
- 每题生成 2—3 条不同采样轨迹；
- 反事实样本扩展至 50 组；
- 人工标注集扩展至 80—100 条。

### 10.3 难度分层

题目分为基础、中等和困难三档。

| 难度 | 分层参考 |
|---|---|
| 基础 | 单一逻辑、少量分支、边界条件较少 |
| 中等 | 多步骤算法、多个边界条件、需要合理数据结构 |
| 困难 | 状态复杂、隐含约束较多、易产生性能或边界错误 |

难度标签来源包括：

1. benchmark 原有信息；
2. 题目所需算法和数据结构；
3. 分支与边界条件数量；
4. 标准实现复杂度；
5. Hy3 基线通过表现。

Hy3 基线表现只用于辅助校准，避免完全以模型是否答对定义题目难度。

### 10.4 可见测试与隐藏测试划分

每道题的测试被划分为：

- **可见测试**：作为题目输入的一部分，帮助理解接口；
- **隐藏测试**：不提供给 Solver，用于评估泛化能力；
- **挑战测试**：针对边界、硬编码和投机行为专门生成；
- **反例测试**：由过程评估器针对具体疑似缺陷生成。

---

## 十一、错误注入与反事实构造

### 11.1 需求与 reasoning 层扰动

- 删除一个必要条件；
- 错误解释输入含义；
- 将“至少”改为“恰好”；
- 使用不满足适用条件的算法；
- 给出错误复杂度结论；
- 添加题目中不存在的假设；
- 将正确步骤替换为无法推出的结论。

### 11.2 代码层扰动

- 比较运算符替换；
- 循环边界偏移；
- 删除边界判断；
- 状态更新顺序调整；
- 错误初始化；
- 返回值替换；
- 删除去重逻辑；
- 将通用逻辑改为硬编码；
- 引入潜在超时实现。

### 11.3 对齐层扰动

- reasoning 声称使用集合，代码改为列表；
- reasoning 声称处理空输入，代码删除对应分支；
- reasoning 声称采用 O(n) 方法，代码改为双重循环；
- 保持代码正确，但将 reasoning 替换为错误解释；
- 保持 reasoning 正确，但注入代码实现偏差。

### 11.4 正确反例

为检验误报率，还需构造不应被判错的样本：

- 与参考答案不同但正确的算法；
- 合理省略显然步骤的简洁 reasoning；
- 使用 Python 语言特性的等价实现；
- 变量命名不同但逻辑一致；
- 边界处理方式不同但行为等价。

---

## 十二、有效性验证方案

官方要求至少验证定位准确率和误报率。本项目将在此基础上增加错误检测、类型分类和可执行证据验证。

### 12.1 功能正确性指标

#### Final Functional Accuracy

```text
通过全部功能测试的代码数量
÷
全部候选代码数量
```

#### Visible Pass / Hidden Fail Rate

统计可见测试通过但隐藏测试失败的比例，用于观察有限测试造成的高估。

### 12.2 过程正确性指标

#### Process Correctness Rate

```text
人工确认推理正确且计划—代码一致的样本数
÷
全部人工标注样本数
```

#### Correct-Result Wrong-Process Recall

```text
成功识别的“结果正确但过程错误”样本数
÷
此类样本总数
```

### 12.3 错误检测指标

- Precision；
- Recall；
- F1；
- 正确样本上的 False Positive Rate；
- 错误样本上的 False Negative Rate。

### 12.4 首错定位指标

#### Exact Localization Accuracy

预测步骤与人工标注首错步骤完全一致。

#### ±1 Step Accuracy

预测步骤与真实步骤相差不超过一步。

#### Layer Localization Accuracy

需求、推理、对齐、代码和执行层级是否判断正确。

### 12.5 错误类型指标

采用：

- Error Type Accuracy；
- Macro-F1；
- 各错误类别召回率；
- 错误类型混淆矩阵。

Macro-F1 可以避免样本数量较多的错误类型掩盖少数类别表现。

### 12.6 错误证据指标

#### Executable Evidence Rate

```text
具有成功执行反例的确认错误数
÷
全部确认错误数
```

#### Counterexample Success Rate

```text
成功找到有效反例的疑似错误数
÷
尝试生成反例的疑似错误数
```

#### Unsupported Claim Rate

统计过程评估器提出但无法由静态或动态证据支持的判断比例。

### 12.7 人工抽检

对以下样本进行重点人工复核：

- 评估器判错但所有测试通过；
- 评估器认为正确但隐藏测试失败；
- LLM 判断与静态分析冲突；
- LLM 判断与执行反例冲突；
- 不同算法但功能正确的样本；
- “结果正确但过程错误”样本。

人工抽检表：

| 样本 ID | 系统判断 | 人工判断 | 是否误报 | 是否有反例 | 备注 |
|---|---|---|---|---|---|
| p001-r1 | 过程错误 | 过程错误 | 否 | 是 | 漏掉重复元素 |
| p014-r2 | 过程错误 | 过程正确 | 是 | 否 | 合法等价实现 |
| p026-r1 | 完全正确 | 实现错误 | 否 | 是 | 隐藏边界失败 |

---

## 十三、对照实验与消融实验

### 13.1 对照方法

| 方法 | 测试执行 | LLM 审查 | 四层对齐 | 静态分析 | 反例验证 |
|---|---:|---:|---:|---:|---:|
| Baseline A：Test-only | ✓ |  |  |  |  |
| Baseline B：Direct Judge | ✓ | ✓ |  |  |  |
| Baseline C：Structured Judge | ✓ | ✓ | ✓ |  |  |
| Baseline D：Static-enhanced | ✓ | ✓ | ✓ | ✓ |  |
| Full Method | ✓ | ✓ | ✓ | ✓ | ✓ |

### 13.2 核心比较问题

实验重点回答以下问题：

1. 只看测试结果会遗漏多少过程错误；
2. 直接 LLM-as-judge 的误报率有多高；
3. 四层对齐是否提升首错定位准确率；
4. 静态证据是否降低计划—代码不一致的漏检；
5. 可执行反例是否能够过滤无法复现的错误判断；
6. 完整方法是否更擅长识别“结果正确但过程错误”；
7. 完整方法是否会误判合法等价实现。

### 13.3 消融实验

在完整方法基础上分别移除：

- 需求条款拆解；
- 推理—代码映射；
- 静态分析；
- 隐藏测试；
- 反例生成；
- 反例执行验证。

比较移除前后的：

- Error Detection F1；
- Exact Localization Accuracy；
- Error Type Macro-F1；
- False Positive Rate；
- Correct-Result Wrong-Process Recall；
- Executable Evidence Rate。

---

## 十四、完整评测执行方案

### 14.1 第一阶段：生成基线解答

对全部基础题目调用 Hy3，保存：

- 原始模型输出；
- 结构化 reasoning；
- 代码；
- 模型参数；
- 时间戳；
- 解析状态。

### 14.2 第二阶段：功能执行

运行：

- 可见测试；
- 隐藏测试；
- 资源限制；
- 异常捕获。

生成全量功能结果。

### 14.3 第三阶段：过程评估

调用四层评估器，输出：

- 推理正确性；
- 计划—代码一致性；
- 首错步骤；
- 首错层级；
- 错误类型；
- 需求条款；
- 代码位置；
- 证据候选。

### 14.4 第四阶段：反例验证

对疑似缺陷：

- 生成候选反例；
- 执行参考实现；
- 执行候选代码；
- 对比结果；
- 缩减输入；
- 更新错误状态。

### 14.5 第五阶段：人工标注与有效性验证

对人工标注子集进行：

- 双维度或三维度标注；
- 首错位置标注；
- 错误类型标注；
- 系统结果对比；
- 误报案例复核。

### 14.6 第六阶段：分层分析

按照以下维度统计：

- 难度；
- 算法类别；
- 错误层级；
- 错误类型；
- reasoning 长度；
- 代码长度；
- 测试通过情况；
- 是否存在可执行反例。

---

## 十五、预期结果与分析内容

本项目不预设具体实验数值，最终根据真实运行结果报告。

预期形成以下分析。

### 15.1 最终答案与过程正确性的差异

比较：

```text
功能正确率
vs
过程正确率
```

重点分析：

- 有多少代码功能正确但过程不成立；
- 有多少 reasoning 正确但代码实现偏离；
- 只看测试通过率会高估多少模型能力。

### 15.2 难度临界点

按基础、中等、困难分层比较：

- 功能正确率；
- 过程正确率；
- 首错定位准确率；
- 错误证据生成成功率。

分析模型在哪一难度区间开始出现显著下降。

### 15.3 典型失败模式

可能分析的模式包括：

- 需求理解正确，但边界条件遗漏；
- reasoning 正确，但代码未实现关键步骤；
- 代码能够通过普通测试，但无法泛化；
- 算法功能正确，但复杂度不满足约束；
- 评估器对合法替代算法发生误报；
- LLM 提出缺陷，但执行证据无法复现；
- 多个错误相互抵消导致最终结果正确。

### 15.4 评估方法贡献

通过消融实验说明：

- 四层对齐是否改善定位；
- 静态分析是否改善对齐判断；
- 动态反例是否降低误报；
- 反事实配对数据是否揭示 test-only 无法发现的问题。

---

## 十六、最终输出数据格式

每条完整结果记录建议采用：

```json
{
  "problem_id": "p001",
  "response_id": "p001_sample_1",
  "difficulty": "medium",
  "visible_tests_passed": true,
  "hidden_tests_passed": false,
  "functional_correct": false,
  "reasoning_correct": true,
  "plan_code_aligned": false,
  "process_correct": false,
  "first_faulty_layer": "implementation",
  "first_faulty_step": "S3",
  "affected_steps": ["S4"],
  "violated_requirement": "R3",
  "code_span": "L18-L21",
  "error_type": "A01",
  "evidence_status": "confirmed_bug",
  "counterexample": {
    "input": [[2, 2, 3]],
    "expected": 1,
    "actual": 4
  },
  "judge_confidence": 0.91,
  "human_verified": true
}
```

---

## 十七、结果可视化

最终报告拟包含以下图表：

1. 不同难度下的功能正确率；
2. 不同难度下的过程正确率；
3. 功能正确率与过程正确率对比；
4. 错误层级分布；
5. 错误类型分布；
6. 首错步骤位置分布；
7. 各方法定位准确率对比；
8. 各方法误报率对比；
9. “结果正确但过程错误”识别召回率；
10. 可执行证据生成成功率；
11. 消融实验结果；
12. 错误类型混淆矩阵。

---

## 十八、项目交付物

### 18.1 开源项目仓库

包括：

- Hy3 解题应用源码；
- 结构化输出解析模块；
- 沙盒执行模块；
- 静态分析模块；
- 四层过程评估模块；
- 反例生成与验证模块；
- 错误定位和分类模块；
- 结果汇总与可视化脚本；
- README；
- 环境配置样例；
- 运行说明。

### 18.2 评测材料

包括：

- 分层题集；
- 标准答案或参考实现；
- 可见与隐藏测试；
- 反事实配对挑战集；
- 人工标注子集；
- 答案校验脚本；
- 过程评估脚本；
- 错误注入脚本；
- 反例生成脚本。

### 18.3 完整结果

包括：

- 最终功能正确率；
- 过程正确率；
- 计划—代码一致率；
- 错误类型分布；
- 难度分层结果；
- 首错定位准确率；
- 误报率；
- “结果正确但过程错误”识别结果；
- 消融实验结果；
- 人工抽检记录。

### 18.4 分析报告

包括：

- 场景选择理由；
- 方法设计依据；
- 四层对齐方法说明；
- 错误分类体系；
- 数据集构造方法；
- 有效性验证实验；
- 典型案例；
- 模型失败模式；
- 模型能力边界；
- 性能临界点；
- 系统局限性；
- 后续改进方向。

### 18.5 Demo 视频或 GIF

时长控制在 2 分钟以内，展示一次完整流程。

---

## 十九、推荐仓库结构

```text
tracejudge-hy3/
├── README.md
├── LICENSE
├── requirements.txt
├── pyproject.toml
├── .env.example
├── Makefile
│
├── configs/
│   ├── default.yaml
│   └── evaluation.yaml
│
├── src/
│   ├── solver/
│   │   ├── hy3_client.py
│   │   ├── prompt_builder.py
│   │   └── response_parser.py
│   │
│   ├── dataset/
│   │   ├── loader.py
│   │   ├── adapter.py
│   │   └── difficulty.py
│   │
│   ├── sandbox/
│   │   ├── executor.py
│   │   ├── limits.py
│   │   └── test_runner.py
│   │
│   ├── static_analysis/
│   │   ├── ast_analyzer.py
│   │   ├── control_flow.py
│   │   └── hardcode_detector.py
│   │
│   ├── evaluator/
│   │   ├── requirement_decomposer.py
│   │   ├── alignment_evaluator.py
│   │   ├── process_judge.py
│   │   ├── error_localizer.py
│   │   ├── taxonomy.py
│   │   └── evidence_aggregator.py
│   │
│   ├── counterexample/
│   │   ├── generator.py
│   │   ├── differential_runner.py
│   │   └── minimizer.py
│   │
│   └── reporting/
│       ├── metrics.py
│       ├── visualization.py
│       └── case_report.py
│
├── data/
│   ├── base/
│   ├── visible_tests/
│   ├── hidden_tests/
│   ├── counterfactual_pairs/
│   └── human_annotations/
│
├── scripts/
│   ├── generate_solutions.py
│   ├── run_tests.py
│   ├── run_process_evaluation.py
│   ├── generate_counterexamples.py
│   ├── validate_evaluator.py
│   └── run_ablation.py
│
├── results/
│   ├── raw/
│   ├── processed/
│   ├── metrics/
│   └── figures/
│
├── docs/
│   ├── proposal.md
│   ├── evaluation_method.md
│   ├── annotation_guideline.md
│   ├── error_taxonomy.md
│   └── final_report.md
│
└── demo/
    ├── demo.gif
    └── demo_script.md
```

推荐提供以下一键命令：

```bash
make demo
make prepare-data
make generate
make evaluate
make validate
make ablation
make report
```

---

## 二十、Demo 设计

Demo 不以展示复杂界面为重点，而是集中展示传统测试评测无法发现的问题。

### 20.1 推荐案例

选择一段满足以下条件的代码：

```text
可见测试全部通过
但遗漏一个关键边界条件
```

系统运行过程：

1. Hy3 生成 reasoning 和代码；
2. 可见测试全部通过；
3. Test-only 方法判定正确；
4. 四层对齐发现：
   - reasoning 声称处理重复元素；
   - 代码中未找到相应逻辑；
5. 反例生成器构造输入；
6. 沙盒执行复现错误；
7. 系统定位到 S3 和代码第 14—17 行；
8. 输出错误类型和可执行错误证书。

### 20.2 两分钟时间分配

| 时间 | 展示内容 |
|---|---|
| 0—15 秒 | 传统测试通过不等于代码真正正确 |
| 15—35 秒 | 输入题目，Hy3 生成结构化解答 |
| 35—55 秒 | 代码通过所有可见测试 |
| 55—80 秒 | 四层对齐发现 reasoning 与代码不一致 |
| 80—105 秒 | 自动生成反例并在沙盒中复现 |
| 105—115 秒 | 展示首错步骤、代码位置和错误类型 |
| 115—120 秒 | 展示整体指标和项目价值 |

---

## 二十一、时间规划

| 时间 | 里程碑 | 核心产出 |
|---|---|---|
| 8/22—8/27 | 方案与基础验证 | 完成方案文档、Hy3 调用、结构化输出原型、题集初筛 |
| 8/28—8/30 | Solver 与数据管线 | 完成题目适配、Hy3 Solver、结构化解析 |
| 8/31—9/2 | 执行与静态分析 | 完成沙盒执行、可见/隐藏测试、AST 分析 |
| 9/3—9/5 | 四层评估器 | 完成需求拆解、步骤对齐、首错定位和错误分类 |
| 9/5—9/7 | 反事实与反例验证 | 完成错误注入、反事实配对、反例生成与执行 |
| 9/7—9/8 | 人工标注与验证 | 完成人工标注子集、定位准确率和误报率实验 |
| 9/8—9/9 | 对照与消融实验 | 完成 Test-only、LLM Judge 和完整方法比较 |
| 9/9—9/10 | 结果分析 | 完成图表、典型案例和模型能力边界分析 |
| 9/10—9/11 | 最终交付 | README、报告、仓库整理、Demo 录制与提交 |

---

## 二十二、风险与应对方案

### 风险一：项目范围过大

**风险：** 同时完成多 Agent、多语言、仓库级任务和自动修复可能无法按时交付。

**应对：**

- 仅支持 Python；
- 仅做函数级任务；
- 优先完成 60 道题的完整闭环；
- 自动修复不作为本阶段必选功能；
- 前端只实现基本结果展示。

### 风险二：LLM 判断不稳定

**风险：** 同一答案多次评估结果不同。

**应对：**

- 使用严格 JSON Schema；
- 固定评估提示词；
- 控制采样参数；
- 引入静态和动态证据；
- 对部分样本重复评估；
- 记录多次评估一致性。

### 风险三：反例生成成功率不足

**风险：** 系统能指出疑似问题，但无法生成有效输入。

**应对：**

- 优先覆盖边界条件类和控制流类错误；
- 结合已有隐藏测试；
- 使用参考实现做差分 oracle；
- 将未复现判断标记为未验证怀疑；
- 不将所有 LLM 判断直接计入确认错误。

### 风险四：参考实现并非唯一正确过程

**风险：** 将与参考实现不同的合法方案误判为错误。

**应对：**

- 不要求与参考代码逐行一致；
- 重点判断是否满足需求和执行行为；
- 在挑战集中加入合法等价实现；
- 使用需求条款而非参考路径作为主要判定依据。

### 风险五：人工标注存在主观差异

**风险：** 首错步骤和错误类型可能存在边界争议。

**应对：**

- 制定标注规范；
- 明确定义首个因果错误；
- 允许记录次级错误和受影响步骤；
- 对争议样本进行复核；
- 报告人工标注一致率或争议比例。

---

## 二十三、最低可交付版本与加分版本

### 23.1 最低可交付版本

必须完成：

- Hy3 结构化代码生成；
- 60 道分层题目；
- 可见与隐藏测试；
- 沙盒执行；
- 需求—推理—代码三层对齐；
- 过程正确性判定；
- 首错步骤定位；
- 错误类型分类；
- 30 组反事实样本；
- 40 条以上人工标注；
- 定位准确率与误报率；
- 完整结果表；
- 分析报告；
- 2 分钟以内 Demo。

### 23.2 加分版本

时间允许时增加：

- 执行轨迹采集；
- 最小反例自动缩减；
- 多次评估一致性分析；
- 复杂度动态验证；
- 多评估器交叉复核；
- 简单可视化界面；
- 自动生成错误修复建议；
- 扩展到 100 道以上题目；
- 每题生成多条不同推理轨迹。

---

## 二十四、合规性说明

1. 项目将在 GitHub 等平台新建公开仓库；
2. README 中明确说明项目为犀牛鸟开源活动个人参赛作品；
3. 不使用容易被误认为腾讯官方项目的命名方式；
4. 模型能力调用通过 Hy3 完成；
5. 本项目不训练或微调模型；
6. API Key 通过环境变量或配置文件传入；
7. 仓库只提交 `.env.example`，不提交真实密钥；
8. README 中提供完整的安装、运行和复现实验说明；
9. 公开数据集保留来源和许可证信息；
10. 自动执行模型代码时使用隔离环境和资源限制。

上述仓库、密钥、项目标识和 Hy3 调用要求均与官方任务说明保持一致。

---

## 二十五、项目核心价值总结

本项目不试图把代码评测包装成一个复杂的多 Agent 系统，而是集中解决一个明确问题：

> 如何判断 AI 生成代码不仅“结果正确”，而且“过程能够支撑结果”，并在存在问题时给出可复现、可追踪的错误证据。

与仅依赖测试通过率或单次 LLM 判断的方法相比，TraceJudge-Hy3 的核心价值在于：

1. **从结果评测升级为过程评测；**
2. **从自由文本判断升级为四层结构化对齐；**
3. **从错误症状定位升级为首个因果错误定位；**
4. **从主观错误解释升级为可执行错误证书；**
5. **从普通 benchmark 升级为反事实配对过程评测集；**
6. **通过对照和消融实验量化每个模块的实际贡献。**

项目的一句话介绍为：

> **TraceJudge-Hy3 不只判断代码是否通过测试，还验证它是否“对因而对”，并为错误结论生成能够执行和复现的证据。**
